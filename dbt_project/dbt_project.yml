name: safe_ai
version: "1.0.0"
config-version: 2

profile: safe_ai

model-paths: ["models"]
test-paths:  ["tests"]
macro-paths: ["macros"]

target-path:   "target"
clean-targets: ["target", "dbt_packages"]

models:
  safe_ai:
    safe:
      +materialized: table
      +schema: safe
