# ECB SAFE microdata ingestion pipeline
# Pin minor versions; patch versions are unconstrained for security updates.

requests>=2.31,<3
beautifulsoup4>=4.12,<5
duckdb>=1.1,<2
pyyaml>=6.0,<7
lxml>=5.0,<6      # faster HTML parser for BeautifulSoup (optional but recommended)
